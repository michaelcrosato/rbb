# Client build setting (public, never a secret). Solo works without a server.
VITE_SERVER_URL=ws://localhost:8787

# Dedicated server settings. Use exact deployed HTTPS origins in production.
PORT=8787
DATA_DIR=./data
WORLD_SEED=quiet-frontier
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4175
